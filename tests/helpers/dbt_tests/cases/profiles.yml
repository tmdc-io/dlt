config:
  # do not track usage, do not create .user.yml
  send_anonymous_usage_stats: False

jaffle_shop:
  target: analytics
  outputs:
    analytics:
      type: postgres
      host: "{{ env_var('NILUS__CREDENTIALS__HOST') }}"
      user: "{{ env_var('NILUS__CREDENTIALS__USERNAME') }}"
      password: "{{ env_var('NILUS__CREDENTIALS__PASSWORD') }}"
      port: 5432
      dbname: "{{ env_var('NILUS__CREDENTIALS__DATABASE') }}"
      schema: "{{ var('dbt_schema') }}"